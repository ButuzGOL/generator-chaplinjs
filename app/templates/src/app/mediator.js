/**
 * Mediator module
 *
 * @module mediator
 */
define([
  'underscore',
  'chaplin'
], function(_, Chaplin) {
  'use strict';

  /**
   * Mediator object
   *
   * @class mediator
   * @static
   * @extends Chaplin.mediator
   */
  var mediator = Chaplin.mediator;

  _.extend(mediator, {
  });

  return mediator;
});