/**
 * Layout module
 *
 * @module views/layout
 */
define([
  'chaplin'
], function(Chaplin) {
  'use strict';

  /**
   * Layout class
   *
   * @class Layout
   * @constructor
   * @extends Chaplin.Layout
   */
  var Layout = Chaplin.Layout.extend({
  });

  return Layout;
});
