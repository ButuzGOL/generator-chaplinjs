/**
 * <%= _.capitalize(name) %> helper module
 *
 * @module helpers/<%= name %>
 */
define([
], function() {
  'use strict';

  /**
   * <%= _.capitalize(name) %> helper
   *
   * @class <%= name %>Helper
   * @static
   */
  return {
  };
});
